rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public access to all collections for this shared-access app
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
